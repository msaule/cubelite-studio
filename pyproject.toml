[project]
name = "cubelite-studio"
version = "0.1.0"
description = "Low-VRAM Cube 3D workflow and benchmarking toolkit for Roblox creators."
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
