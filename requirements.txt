streamlit>=1.34
torch>=2.1
trimesh>=4.0
psutil>=5.9
pandas>=2.0
pytest>=8.0
pynvml>=11.5
plotly>=5.18
matplotlib>=3.8

# Optional for mesh simplification. It can be heavy on some systems, so install manually if needed:
# pymeshlab>=2023.12
